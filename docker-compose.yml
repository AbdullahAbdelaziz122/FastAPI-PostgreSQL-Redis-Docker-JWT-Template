version: '3.8'

services:
  # 1. PostgreSQL Database
  db:
    image: postgres:15-alpine
    container_name: #container-name-here
    restart: always
    environment:
      - POSTGRES_USER= #username
      - POSTGRES_PASSWORD= #password
      - POSTGRES_DB= #Database Name
    ports:
      - "5433:5432" 
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U abd -d trendsight"]
      interval: 5s
      timeout: 5s
      retries: 5

  # 2. Redis Cache
  redis:
    image: redis:7-alpine
    container_name: #redis-container-name
    restart: always
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 3

  # 3. FastAPI Backend
  web:
    build: .
    container_name: #name
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
        #change actual url or add from .env 
      - DATABASE_URL=postgresql+asyncpg://username:password@db:5432/trendsight
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      
      - ./app:/app/app

volumes:
  postgres_data:
